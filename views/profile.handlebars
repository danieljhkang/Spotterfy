<main>
    <div id="user-info">
        <h3>Name</h3>
        <p>{{fullName}}</p>
        <h3>Email</h3>
        <p>{{email}}</p>
        <h3>CWID</h3>
        <p>{{cwid}}</p>
    </div>
    {{#if weeklyCheckIns}}
    <p>You've checked in {{weeklyCheckIns}} times this week!</p>
    {{else}}
    <p>Looks like you haven't checked in this week yet!</p>
    {{/if}}
    
    {{#if monthlyMissedReservations}}
    <p>You've missed {{monthlyMissedReservations}} reservations this month!</p>
    {{else}}
    <p>You haven't missed a reservation this month!</p>
    {{/if}}

    <form action="/profile" method="POST" id="switch-visibility">
        {{#if visible}}
            <button type="submit">Change to Private</button>
        {{else}}
            <button type="submit">Change to Public</button>
        {{/if}}
    </form>
    <button>
        <a href="/logout">Log Out</a>
    </button>
</main>
